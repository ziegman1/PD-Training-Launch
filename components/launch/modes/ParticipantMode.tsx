/**
 * Participant mode — lightweight companion workbook (reflection, exercises).
 * Secondary to live screen share; same deck index when `syncDeck` is enabled.
 * @see WorkbookView
 */
export { WorkbookView as ParticipantMode } from "@/components/launch/views/WorkbookView";
